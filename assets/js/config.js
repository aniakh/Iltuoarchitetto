/* Il Tuo Architetto — runtime config for the demo.
 *
 * Set GEMINI_PROXY to your Cloudflare Worker URL after deploying
 * worker/gemini-proxy.js. While the value is empty, the demo asks each
 * visitor to enter their own Google AI Studio API key in the header.
 */
window.GEMINI_PROXY = "https://iltuoarchitetto-proxy.ania-khodabakhshian.workers.dev";
